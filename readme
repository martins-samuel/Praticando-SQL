## 📁 Módulo 1:Praticando SQL => realizando consultas

Este diretório contém os exercícios práticos focados nos comandos fundamentais para realizar consultas em SQL.

### Tópicos Abordados
* Seleção de dados com `SELECT` e `FROM`
* Criação de condições com `WHERE`
* Limitação de resultados com `LIMIT`
* Ordenação de resultados com `ORDER BY`

---

### ⚠️ Importante: Ordem de Execução

Para criar o banco de dados e popular os dados corretamente, os scripts **devem** ser executados na seguinte ordem:

1.  `Criando_tabelas.sql` (Cria a estrutura do banco)
2.  `Inserindo_os_dados.sql` (Insere os dados nas tabelas)

Após executar esses dois primeiros arquivos, os demais scripts (exercícios 1 a 10) podem ser executados em qualquer ordem para praticar as consultas.
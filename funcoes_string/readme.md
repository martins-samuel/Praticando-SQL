# 📁 Módulo 03: Praticando SQL Funções de String

Este diretório contém os exercícios práticos focados na manipulação de dados textuais.

O foco aqui é relembrar e praticar o uso de funções essenciais de string para tratar, formatar, extrair e padronizar dados em suas consultas.

### 🛠️ Funções Essenciais Abordadas
* **`CONCAT()`**: Para juntar strings, como nome e sobrenome, em uma única coluna.
* **`LENGTH()`**: Para descobrir o comprimento de uma string, útil para validar dados.
* **`SUBSTRING()`**: Para extrair partes específicas de uma string, como um código de área.
* **`LOWER()`**: Para converter strings para minúsculas, garantindo a padronização.
* **`TRIM()`**: Para remover espaços desnecessários no início ou no final de uma string.
* **`REPLACE()`**: Para substituir partes de uma string por outras, permitindo correções rápidas.

O objetivo é entender como aplicar essas funções em diferentes contextos e como elas funcionam na prática.

---

### ✨ Técnicas Adicionais

Além das funções individuais, também foi praticada a **combinação de funções** para realizar tarefas mais complexas.

* **Exemplo:** Usar `LOWER(TRIM(coluna))` para formatar descrições de produtos, removendo espaços extras e convertendo para minúsculas de uma só vez.
### ⚠️ Ordem de Execução (Se Aplicável)



Scripts de criação do banco de dados e prenchimento, siga a ordem:



1.  `criacao_db.sql` (Cria a estrutura)

2.  `preenchimento_db.sql` (Insere os dados)
# 📊 Funções Numéricas (SQL)

Este diretório é dedicado aos aprendizados do Módulo 5 do curso "Praticando SQL: entendendo funções numéricas". O objetivo é demonstrar o uso de funções e operadores essenciais para manipular e analisar dados numéricos diretamente nas consultas SQL.

---

## 🧮 Funções e Operadores Abordados

* **Operadores Aritméticos (+, -, *, /, %)**
    * Funções básicas para soma, subtração, multiplicação, divisão e módulo (resto da divisão).

* **ROUND()**
    * Arredonda um número para um número específico de casas decimais.

* **CEIL()**
    * Retorna o menor inteiro maior ou igual ao número (arredonda para cima).

* **FLOOR()**
    * Retorna o maior inteiro menor ou igual ao número (arredonda para baixo).

* **ABS()**
    * Retorna o valor absoluto (positivo) de um número, removendo o sinal negativo.

* **POWER()**
    * Retorna um número elevado a uma potência específica.

* **SQRT()**
    * Retorna a raiz quadrada de um número.
### ⚠️ Importante: Ordem de Execução

Para criar o banco de dados e popular os dados corretamente, os scripts **devem** ser executados na seguinte ordem:

1.  `script_criacao_db.sql` (Cria a estrutura do banco)
2.  `script_preenchimento_db.sql` (Insere os dados nas tabelas)

Após executar esses dois primeiros arquivos, os demais scripts (exercícios 1 a 10) podem ser executados em qualquer ordem para praticar as consultas.
